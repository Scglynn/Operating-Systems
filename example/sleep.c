#include "types.h"
#include "user.h"

int
main(int argc, char const *argv[])
{
    //int ticks = 100;
    // if (argc > 2)
    // {
    //     printf(1,"usage: sleep [ticks]\n");
    //     exit();
    // }
    // if(argc == 2)
    //     ticks = atoi(argv[1]);

    sleep(100);
    exit();
}
