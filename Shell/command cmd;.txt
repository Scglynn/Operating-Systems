scommand cmd;

char*line;
char buffer[1000];

int rc = parse(line,&cmd);


if(rc==0){

    //you canuse cmd...

    cmd.argv[0]
    cmd.argc == 2;
    strcmp(cmd.output == "out.txt") == 0

    cmd.bg == 1
}

ls *.txt > out.txt