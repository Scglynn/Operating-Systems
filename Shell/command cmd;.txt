command cmd;

char*line;
char buffer[1000];

int rc = parse(line,&cmd);


if(rc==0){

    //you canuse cmd...

    cmd.argv[0]
    cmd.argc == 2;
    strcmp(cmd.output == "out.txt") == 0

    cmd.bg == 1
}

ls *.txt > out.txt







shell >     ls > dir1.txt
shell >     & ls > dir2.txt
shell >     & echo hi mom > hi.txt
shell>      bg
6 7 8 
shell>

echo hi mom | wc | wc | cat.....(we don't have to worry about)